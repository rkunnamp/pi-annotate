// Re-export shared types from pi extension
export type {
  Annotation,
  DetailLevel,
  AnnotationResult,
  ToolbarSettings,
  DemoAnnotation,
  SocketMessage,
} from "../../types.js";

// Re-export the constant (not just the type)
export { DEFAULT_TOOLBAR_SETTINGS } from "../../types.js";
